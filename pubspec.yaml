name: mdeditor
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  bitsdojo_window: ^0.1.6
  cupertino_icons: ^1.0.6
  file_picker: ^8.3.3
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.3
  gap: ^3.0.1
  get: 4.6.6
  markdown_to_pdf:
    git: https://github.com/datagrove/markdown_to_pdf.git
  markdown_widget: ^2.0.0+1
  multi_split_view: ^3.6.0
  path_provider: ^2.1.2

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
